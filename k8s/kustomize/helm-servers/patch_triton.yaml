apiVersion: mlops.seldon.io/v1alpha1
kind: Server
metadata:
  name: triton
  labels:
    HACK_REMOVE_ME_TRITON: HACK_REMOVE_ME_TRITON
spec:
  replicas: HACK_REMOVE_ME{{ .Values.triton.replicas }}
  podSpec: HACK_REMOVE_ME{{ toJson .Values.triton.podSpec }}
