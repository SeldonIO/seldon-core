apiVersion: mlops.seldon.io/v1alpha1
kind: Experiment
metadata:
  name: add1020
  namespace: seldon-mesh
spec:
  default: add10
  candidates:
  - modelName: add10
    weight: 50
  - modelName: add20
    weight: 50
