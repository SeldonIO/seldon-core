[tool:pytest]
markers =
    sequential: mark a test as a test which needs to be run sequentially (as opposed to in parallel).
    notebooks: mark a test as a Jupyter notebook test, which are run separately.
