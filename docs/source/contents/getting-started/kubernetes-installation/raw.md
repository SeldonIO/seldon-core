# Raw Yaml Install

We provide raw yaml to install the CRDs and components.

## Install

From the project root run:

```
make deploy-k8s
```

## Uninstall

From the project root run:

```
make undeploy-k8s
```
