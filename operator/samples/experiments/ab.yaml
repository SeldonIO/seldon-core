apiVersion: mlops.seldon.io/v1alpha1
kind: Experiment
metadata:
  name: experiment-iris
  namespace: seldon-mesh
spec:
  candidates:
  - modelName: iris
    weight: 50
  - modelName: iris2
    weight: 50

