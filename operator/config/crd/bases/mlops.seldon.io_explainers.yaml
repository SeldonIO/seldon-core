
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: explainers.mlops.seldon.io
spec:
  group: mlops.seldon.io
  names:
    kind: Explainer
    listKind: ExplainerList
    plural: explainers
    singular: explainer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Explainer is the Schema for the explainers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ExplainerSpec defines the desired state of Explainer
            properties:
              blackbox:
                properties:
                  modelRef:
                    description: Reference to Model for Black Box models
                    type: string
                  pipelineRef:
                    description: Reference to Pipeline for Black Box models
                    type: string
                  pipelineStep:
                    description: Reference to Pipeline step - must include pipeline
                    type: string
                type: object
              name:
                description: Name override
                type: string
              resources:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Resources needed for artifact, cpu, memory etc
                type: object
              schemaUri:
                description: Schme URI
                type: string
              secret:
                description: Secret
                type: string
              serviceAccountName:
                description: Service Account Name for auth
                type: string
              shared:
                description: Whether the artifact can be run on a shared server
                type: boolean
              storageUri:
                description: Storage URI for the model repository
                type: string
              type:
                description: The artifact type e.g. Alibi If not provided then Alibi
                  is assumed
                type: string
              whitebox:
                type: object
            type: object
          status:
            description: ExplainerStatus defines the observed state of Explainer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
