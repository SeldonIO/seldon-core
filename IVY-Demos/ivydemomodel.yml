apiVersion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata:
  name: ivydemomodel
  namespace: seldon-system
spec:
  name: ivydemomodel
  predictors:
  - componentSpecs:
    - spec:
        containers:
        - name: ivydemomodel
          image: ivydemomodel:latest
    graph:
      name: ivydemomodel
    name: test
    replicas: 1
