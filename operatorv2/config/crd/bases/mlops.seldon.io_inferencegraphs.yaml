
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: inferencegraphs.mlops.seldon.io
spec:
  group: mlops.seldon.io
  names:
    kind: InferenceGraph
    listKind: InferenceGraphList
    plural: inferencegraphs
    singular: inferencegraph
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceGraph is the Schema for the inferencegraphs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InferenceGraphSpec defines the desired state of InferenceGraph
            properties:
              explainers:
                description: The associated explainers for this inference graph
                items:
                  properties:
                    end:
                      description: End step name. If omitted will assume last step
                      type: string
                    spec:
                      description: Specification of explainer
                      properties:
                        modelRef:
                          description: Reference to model InferenceArtifact for Black
                            Box models
                          type: string
                        resources:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: Resources needed for artifact, cpu, memory
                            etc
                          type: object
                        server:
                          description: Name of the InferenceServer to deploy this
                            artifact.
                          type: string
                        shared:
                          description: Whether the artifact can be run on a shared
                            server
                          type: boolean
                        storageUri:
                          description: The storage location of the saved explainer
                          type: string
                        type:
                          description: The artifact type e.g. Alibi If not provided
                            then Alibi is assumed
                          type: string
                      type: object
                    start:
                      description: Start step name. If omitted will assume first step
                      type: string
                  required:
                  - spec
                  type: object
                type: array
              steps:
                description: The steps of this inference graph pipeline
                items:
                  properties:
                    if:
                      description: Condition specification to satisfy for this step
                        to be run
                      type: string
                    name:
                      description: Name of the step
                      type: string
                    next:
                      description: Next synchronous step
                      items:
                        type: string
                      type: array
                    nextAsync:
                      description: Next asynchronous step
                      items:
                        type: string
                      type: array
                    ref:
                      description: Reference to model artifact
                      type: string
                    spec:
                      description: Model artifact spec inline
                      properties:
                        memory:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        requirements:
                          description: List of extra requirements for this model to
                            be loaded on a compatible server, e.g. sklearn19
                          items:
                            type: string
                          type: array
                        resources:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: Resources needed for artifact, cpu, memory
                            etc
                          type: object
                        server:
                          description: Name of the InferenceServer to deploy this
                            artifact
                          type: string
                        shared:
                          description: Whether the artifact can be run on a shared
                            server
                          type: boolean
                        storageUri:
                          description: Storage URI for the model repository
                          type: string
                        type:
                          description: The artifact type sklearn, pytorch, alibi-explain,
                            alibi-detect
                          type: string
                      required:
                      - memory
                      - storageUri
                      type: object
                  required:
                  - name
                  type: object
                type: array
            required:
            - steps
            type: object
          status:
            description: InferenceGraphStatus defines the observed state of InferenceGraph
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
