
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: inferenceexplainers.mlops.seldon.io
spec:
  group: mlops.seldon.io
  names:
    kind: InferenceExplainer
    listKind: InferenceExplainerList
    plural: inferenceexplainers
    singular: inferenceexplainer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InferenceExplainer is the Schema for the inferenceexplainers
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InferenceExplainerSpec defines the desired state of InferenceExplainer
            properties:
              modelRef:
                description: Reference to model InferenceArtifact for Black Box models
                type: string
              resources:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Resources needed for artifact, cpu, memory etc
                type: object
              server:
                description: Name of the InferenceServer to deploy this artifact.
                type: string
              shared:
                description: Whether the artifact can be run on a shared server
                type: boolean
              storageUri:
                description: The storage location of the saved explainer
                type: string
              type:
                description: The artifact type e.g. Alibi If not provided then Alibi
                  is assumed
                type: string
            type: object
          status:
            description: InferenceExplainerStatus defines the observed state of InferenceExplainer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
