apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app: seldon
    app.kubernetes.io/instance: seldon-core
    app.kubernetes.io/name: seldon-core-operator
  name: seldon-manager-psp
rules:
- apiGroups:
  - policy
  resourceNames:
  - domino-unrestricted
  resources:
  - podsecuritypolicies
  verbs:
  - use
