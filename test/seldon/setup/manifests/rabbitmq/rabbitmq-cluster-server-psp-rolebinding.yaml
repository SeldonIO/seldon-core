apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app: rabbitmq
    app.kubernetes.io/instance: rabbitmq-cluster
    app.kubernetes.io/name: rabbitmq-cluster-server
  name: rabbitmq-cluster-server-psp
  namespace: rabbitmq
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: rabbitmq-cluster-server-psp
subjects:
- kind: ServiceAccount
  name: rabbitmq-cluster-server
  namespace: rabbitmq
