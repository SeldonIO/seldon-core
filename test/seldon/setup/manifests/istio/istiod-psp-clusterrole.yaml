apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app: istiod
    app.kubernetes.io/name: istiod
    istio: ingressgateway
  name: istiod-psp
rules:
- apiGroups:
  - policy
  resourceNames:
  - domino-unrestricted
  resources:
  - podsecuritypolicies
  verbs:
  - use
