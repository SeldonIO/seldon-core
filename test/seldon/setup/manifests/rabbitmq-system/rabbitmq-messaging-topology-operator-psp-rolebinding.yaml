apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app: rabbitmq
    app.kubernetes.io/instance: rabbitmq-cluster
    app.kubernetes.io/name: rabbitmq-messaging-topology-operator
  name: rabbitmq-messaging-topology-operator-psp
  namespace: rabbitmq-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: rabbitmq-messaging-topology-operator-psp
subjects:
- kind: ServiceAccount
  name: rabbitmq-cluster-rabbitmq-messaging-topology-operator
  namespace: rabbitmq-system
