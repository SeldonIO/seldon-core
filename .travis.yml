sudo: required

services:
  - docker

branches:
  only:
    - master
    - travis-update

jobs:
  include:
    - stage: build-components
      script: echo item 1
    - # same stage
      script: echo item 2

