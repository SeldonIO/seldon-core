name: model-a

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      alpha: float
      l1_ratio: { type: float, default: 0.1 }
    command: spark-submit train.py --alpha {alpha} --l1_ratio {l1_ratio}
