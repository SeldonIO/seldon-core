First install eventing.

(Try in-mem as with kafka am hitting hitting https://github.com/knative/eventing/issues/1383)

Create broker:

kubectl label namespace default knative-eventing-injection=enabled
kubectl -n default get broker default

Create service:

kubectl apply -f knative-example-service.yaml

And trigger:

kubectl apply -f ./trigger.yaml

To send event first create an interactive pod in the cluster:

kubectl run curl --image=radial/busyboxplus:curl -i --tty --rm

Then from that session run:

curl -v "http://default-broker.default.svc.cluster.local/" \
  -X POST \
  -H "X-B3-Flags: 1" \
  -H 'CE-SpecVersion: 0.2' \
  -H "CE-Type: dev.knative.foo.bar" \
  -H "CE-Time: 2018-04-05T03:56:24Z" \
  -H "CE-ID: 45a8b444-3213-4758-be3f-540bf93f85ff" \
  -H "CE-Source: dev.knative.example" \
  -H 'Content-Type: application/json' \
  -d '{ "much": "wow" }'


TODO:
Need to send request to broker from seldon-engine.
And parse and transform it from seldon-request-logger.