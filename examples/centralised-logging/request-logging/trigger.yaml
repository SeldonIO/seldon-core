apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: seldon-message-logging-trigger
  namespace: default
spec:
  filter:
    sourceAndType:
      type: seldon.message.pair
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1alpha1
      kind: Service
      name: seldon-message-logging