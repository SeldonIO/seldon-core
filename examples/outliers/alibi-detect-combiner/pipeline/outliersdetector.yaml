apiVersion: machinelearning.seldon.io/v1alpha2
kind: SeldonDeployment
metadata:
  labels:
    app: seldon
  name: outliersdetector
spec:
  name: outliersdetector
  predictors:
  - componentSpecs:
    - spec:
        containers:
        - image: rafalskolasinski/seldon-experiments:outliersdetector-0.1
          name: model-outliers
          imagePullPolicy: Always
    graph:
      children: []
      name: model-outliers
      type: MODEL
      endpoint:
        type: REST
    name: outliersdetector
    replicas: 1
